#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(int argc, const char *argv[])
{
    struct tm *t;
    int y, m, d, sd, sy;
    while (scanf("%4d/%2d/%2d", &y, &m, &d) != EOF) {
        t = malloc(sizeof(struct tm));
    }
    return 0;
}
